{
  "name": "mimo-tts-webui",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "start": "cd server && NODE_ENV=production node dist/index.js",
    "clean": "rm -rf node_modules client/node_modules server/node_modules client/dist server/dist package-lock.json client/package-lock.json server/package-lock.json client/tsconfig.tsbuildinfo",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
